import { FIREBASE_DB } from "@/FirebaseConfig";
import { query } from "firebase/firestore";

const Fetch = (email: string) => {
  try {
  } catch (er) {
    console.error("Couldn't fetch favorites ", er);
  }
};
